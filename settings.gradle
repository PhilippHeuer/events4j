include 'api', 'core', 'handler-reactor', 'handler-simple', 'handler-spring', 'bridge-discord4j', 'bridge-jda'

rootProject.name = 'events4j'
rootProject.children.each {
    it.name = rootProject.name + "-" + it.name
}
